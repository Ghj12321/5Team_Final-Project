@* <button id="logoutButton">로그아웃</button> *@

@* <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<script>
    $(document).ready(function () {
        $("#logoutButton").click(function () {
            $.ajax({
                type: "POST",
                url: "/YourController/Logout", // 실제 로그아웃을 처리하는 URL로 변경하세요.
                success: function (response) {
                    if (response.success) {
                        alert(response.message);
                        // 로그아웃 후 필요한 추가 처리를 여기에 작성하세요.
                        // 예: 로그인 페이지로 리다이렉트
                        window.location.href = "/LoginPageUrl"; // 실제 로그인 페이지 URL로 변경하세요.
                    }
                },
                error: function () {
                    alert("로그아웃 요청 처리 중 오류가 발생했습니다.");
                }
            });
        });
    });
</script> *@

